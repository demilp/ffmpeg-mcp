{
  "name": "ffmpeg-mcp-server",
  "version": "2.0.0",
  "description": "Enhanced FFmpeg MCP Server for multimedia processing with security, caching, and batch operations",
  "type": "module",
  "main": "build/index.js",
  "bin": {
    "ffmpeg-mcp-server": "build/index.js"
  },
  "scripts": {
    "build": "tsc",
    "start": "node build/index.js",
    "dev": "ts-node src/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rm -rf build logs",
    "lint": "tslint src/**/*.ts",
    "watch": "tsc --watch"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.4.0",
    "glob": "^10.3.10"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0",
    "@types/glob": "^8.1.0",
    "ts-node": "^10.0.0",
    "tslint": "^6.1.3"
  },
  "keywords": [
    "ffmpeg",
    "mcp",
    "video",
    "audio",
    "processing",
    "multimedia",
    "batch",
    "streaming"
  ],
  "author": "FFmpeg MCP Team",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  }
}
